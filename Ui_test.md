# UI Testing Report: Camera Rental System

**Session Date**: 2026-04-21  
**Environment**: Production (Localhost)  
**Test Suite**: 5 Functional Core Flows  
**Status**: 🟢 100% Pass

---

## 📊 Summary Table

| Test ID | Feature | Expected Result | Actual Result | Status |
|:---:|:---|:---|:---|:---:|
| **TC001** | Homepage | Logo & Hero text should be visible and responsive. | Found "CAPTURE MOMENTS" and Nav bar successfully. | ✅ |
| **TC002** | Registration | User should be redirected to `/browse` and session should start. | User registered and "Logout" button appeared in Nav. | ✅ |
| **TC003** | Search | Search for "EOS R5" should display relevant items. | Filtered results showing "EOS R5" camera cards. | ✅ |
| **TC004** | Product Info | Pricing (฿) and details should be visible on cards. | Camera cards and price symbol detected correctly. | ✅ |
| **TC005** | Booking Flow | Dates should be typable; booking should land in Cart. | Dates entered; redirected to `/cart` with "pending" status. | ✅ |

---

## 🔍 Detailed Test Cases & Expected Results

### TC001: Homepage Performance & Visibility
- **Objective**: Ensure the entry point is functional.
- **Expected Result**: 
  - [x] Page status is 200 OK.
  - [x] Hero text "CAPTURE MOMENTS" is prominent.
  - [x] Navigation bar is sticky and clickable.
- **Actual Result**: PASSED. Render time < 2s.

### TC002: User Lifecycle (Signup)
- **Objective**: Validate the registration pipeline.
- **Expected Result**: 
  - [x] Redirect to `/browse` upon success.
  - [x] A successful session cookie is set (verified by checking for Logout link).
- **Actual Result**: PASSED. Registration with random hash UID completed successfully.

### TC003: Dynamic Search Filtering
- **Objective**: Verify backend filtering logic.
- **Expected Result**: 
  - [x] Results must contain the keyword "EOS R5".
  - [x] Non-matching cameras should be hidden.
- **Actual Result**: PASSED. Search returned specific model card.

### TC004: UI Data Binding (Camera Metadata)
- **Objective**: Ensure pricing and model data are bound to the view.
- **Expected Result**: 
  - [x] Price per day must be visible using the ฿ symbol.
  - [x] "Reserve Now" buttons should be enabled.
- **Actual Result**: PASSED. Data structure matches inventory values.

### TC005: Booking Logic & Redirection
- **Objective**: Test the end-to-end booking intent flow.
- **Expected Result**: 
  - [x] `flatpickr` inputs should accept YYYY-MM-DD strings.
  - [x] Clicking "Reserve Now" should create a `pending` rental.
  - [x] Final destination must be the `/cart` page.
- **Actual Result**: PASSED. Redirected to cart with the newly created booking visible.

---

## 🛠 Fixes Implemented During Testing
1. **RegEx Domain Validation**: Adjusted test data to use `@gmail.com` to pass strict server-side validation.
2. **Flatpickr Targeting**: Updated selectors from `input[type="date"]` to `input.date-input` to match the actual DOM generated by the datepicker library.
3. **Session Awareness**: Added Mock Google Login steps to TC005 to ensure the booking flow doesn't hit the 401 Unauthorized block.

**Generated by Antigravity AI**
